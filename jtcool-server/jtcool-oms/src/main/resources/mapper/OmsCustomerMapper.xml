<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jtcool.oms.mapper.OmsCustomerMapper">
    
    <resultMap type="com.jtcool.oms.domain.OmsCustomer" id="OmsCustomerResult">
        <result property="customerId" column="customer_id"/>
        <result property="customerCode" column="customer_code"/>
        <result property="customerName" column="customer_name"/>
        <result property="customerType" column="customer_type"/>
        <result property="contactPerson" column="contact_person"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="contactEmail" column="contact_email"/>
        <result property="address" column="address"/>
        <result property="creditLimit" column="credit_limit"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectOmsCustomerVo">
        select customer_id, customer_code, customer_name, customer_type, contact_person, contact_phone, 
        contact_email, address, credit_limit, status, del_flag, create_by, create_time, update_by, update_time, remark 
        from oms_customer
    </sql>

    <select id="selectOmsCustomerList" parameterType="com.jtcool.oms.domain.OmsCustomer" resultMap="OmsCustomerResult">
        <include refid="selectOmsCustomerVo"/>
        <where>
            del_flag = '0'
            <if test="customerName != null and customerName != ''">
                AND customer_name like concat('%', #{customerName}, '%')
            </if>
            <if test="customerCode != null and customerCode != ''">
                AND customer_code = #{customerCode}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="selectOmsCustomerById" parameterType="Long" resultMap="OmsCustomerResult">
        <include refid="selectOmsCustomerVo"/>
        where customer_id = #{customerId}
    </select>

    <insert id="insertOmsCustomer" parameterType="com.jtcool.oms.domain.OmsCustomer">
        insert into oms_customer(customer_code, customer_name, customer_type, contact_person, contact_phone, 
        contact_email, address, credit_limit, status, create_by, create_time, remark)
        values(#{customerCode}, #{customerName}, #{customerType}, #{contactPerson}, #{contactPhone}, 
        #{contactEmail}, #{address}, #{creditLimit}, #{status}, #{createBy}, CURRENT_TIMESTAMP, #{remark})
    </insert>

    <update id="updateOmsCustomer" parameterType="com.jtcool.oms.domain.OmsCustomer">
        update oms_customer
        <set>
            <if test="customerName != null">customer_name = #{customerName},</if>
            <if test="contactPerson != null">contact_person = #{contactPerson},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="contactEmail != null">contact_email = #{contactEmail},</if>
            <if test="address != null">address = #{address},</if>
            <if test="creditLimit != null">credit_limit = #{creditLimit},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = CURRENT_TIMESTAMP,
            <if test="remark != null">remark = #{remark},</if>
        </set>
        where customer_id = #{customerId}
    </update>

    <delete id="deleteOmsCustomerByIds" parameterType="Long">
        update oms_customer set del_flag = '2' where customer_id in
        <foreach item="customerId" collection="array" open="(" separator="," close=")">
            #{customerId}
        </foreach>
    </delete>

</mapper>
